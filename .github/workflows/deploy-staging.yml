name: Stage the app
on:
  pull_request:
    types: [labeled]
jobs:
  build:
    runs-on: ubuntu-latest
    if: contains(github.event.pull_request.labels.*.name, 'stage')
    steps:
      - uses: actions/checkout@v4
      - name: Build and stage
        run: echo "Add your build/staging steps here"
